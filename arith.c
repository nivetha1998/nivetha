#include<stdio.h>
main()
{
int a,n,d,tn,sum=0;
scanf("%d%d%d",&a,&n,&d);
sum=(n*(2*a+(n-1)*d))/2;
tn=a+(n-1)*d;
for(i=a;i<=tn;i=i+d)
{
if(i!=tn)
  {
    printf("%d",i);
   }
 else
 {
 printf("%d=%d",i,sum);
 }
 }
 }
